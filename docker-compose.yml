version: '3'
services:
        web:
                build: .
                environment:
                        - REDIS_HOST=redis
                ports:
                        - "4005:3000"
                depends_on:
                        - redis
        redis:
                image: redis
